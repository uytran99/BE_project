# Environment Variables for Production

# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your_super_secure_jwt_secret_here

# MongoDB Connection String (DO NOT commit real credentials)
# Format (SRV / Atlas): mongodb+srv://<username>:<password>@<cluster-host>/<database>?retryWrites=true&w=majority
# Encode special characters in password with URL encoding (e.g. ! -> %21)
# Example: mongodb+srv://heart_user:Sup3r%21Secret@cluster0.xxxxx.mongodb.net/heart_rate
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/heart_rate_db?retryWrites=true&w=majority

# Node Environment
NODE_ENV=production

# Port (Render/Railway may set automatically; override only if needed for local dev)
# PORT=3000

# Optional: Python path (usually auto-detected - omit unless you know you need it)
# PYTHONPATH=/usr/local/bin/python3

# --- Notes ---
# 1. Copy to .env and fill values.
# 2. Rotate credentials if they were ever exposed publicly.
# 3. Never log the full MongoDB URI; server only reports connection type.